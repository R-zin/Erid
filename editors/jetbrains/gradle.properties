# Keep the Kotlin stdlib off the compile classpath: the plugin is pure Java and
# the IntelliJ Platform Gradle Plugin otherwise injects it by default.
kotlin.stdlib.default.dependency=false

org.gradle.caching=true
org.gradle.parallel=true
