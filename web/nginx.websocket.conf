# WebSocket upgrade mapping for the `api` proxy. The `map` directive must live
# in the http context (nginx includes conf.d/*.conf inside http), so it sits in
# its own file. Empty Upgrade header → "close", otherwise "upgrade".
map $http_upgrade $connection_upgrade {
    default upgrade;
    "" close;
}
