---
name: hub:decisions
description: List the most recent decisions recorded in the shared workspace.
argument-hint: "[limit]"
---

Call the context-hub `recent_decisions` tool (use $ARGUMENTS as the limit if given). Present each
decision's title, the reason, any related files, and who recorded it, newest first.
