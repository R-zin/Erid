---
name: hub:catch-up
description: Brief a fresh session on the workspace so it doesn't start from zero.
---

You just joined this workspace and need to catch up. Call the context-hub `workspace_summary`,
`recent_decisions`, and `active_developers` tools, then brief the user on: the project's goal as
implied by open tasks, the key decisions already made (so you don't re-litigate them), who's active,
and where you could pick up work.
